import React, { useState } from 'react';
const EmployeeProjectLocation: React.FC<{ address?: string }> = ({ address }) => { const [copied, setCopied] = useState(false); if (!address || address === 'Address not specified') return <section className="card"><h2 className="font-bold">Project location</h2><p className="mt-2 text-sm text-gray-600">Your contractor has not added a service address yet. Contact them for directions.</p></section>; const encoded = encodeURIComponent(address); const copy = async () => { try { await navigator.clipboard.writeText(address); setCopied(true); } catch { window.prompt('Copy the project address:', address); } }; return <section className="card"><h2 className="font-bold">Project location</h2><p className="mt-2 text-sm text-gray-700">{address}</p><div className="mt-3 flex flex-wrap gap-2"><a className="secondary" href={`https://www.google.com/maps/search/?api=1&query=${encoded}`} target="_blank" rel="noreferrer">Google Maps</a><a className="secondary" href={`https://maps.apple.com/?q=${encoded}`} target="_blank" rel="noreferrer">Apple Maps</a><button className="secondary" onClick={() => void copy()}>{copied ? 'Copied' : 'Copy link'}</button></div></section>; };
export default EmployeeProjectLocation;
