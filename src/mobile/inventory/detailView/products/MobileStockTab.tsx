// src/mobile / inventory / detailView / products / MobileStockTab.tsx
import React from 'react';
const MobileStockTab: React.FC<{ disabled?: boolean }> = () => (
    <div className="p-4 text-sm text-gray-400 text-center mt-8">Stock tab — coming soon</div>
);
export default MobileStockTab;